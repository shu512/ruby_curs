<p>
  <strong><%= question.id.to_s + ':' %></strong>
  <%= question.content %>
  <%= link_to 'edit', edit_discipline_question_path(question) %> |
  <%= link_to 'destroy', [question.discipline, question],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
</p>