
<table>
  <tr>
    <th>№</th>
    <th>FIO</th>
  </tr>
</table>
<% @students.each_with_index do |student, index| %>
  <%= render student %>
<% end %>

<%= link_to 'New students', new_student_path %>